val pico = require "pico"

val BOARD = "data/images/core/menu/blackboard.png"
val LOGO  = "data/images/core/misc/logo.png"
val NEXT  = "data/images/core/misc/next.png"

val TITLE = "The Journey Begins"
val TEXT = [
    "For a long time, the Pingus have lived happily in peace on the",
    "South Pole along with all the other animals. Everything was in",
    "balance and it seemed like nothing could disrupt their peace.",
    "The Pingus were happy and it seemed like this could never end.",
]

task () {
    watching :key.dn [key='Escape'] {
        loop on :draw {
            ;; board fills the window
            pico.output.draw.image(BOARD, ['%', x=0.5, y=0.5, w=1, h=1])
            pico.set.pencil [ color="white" ]
            ;; title (top center)
            pico.output.draw.text(TITLE, ['%', x=0.5, y=0.13, h=0.06])
            ;; placeholder drawing (upper middle)
            pico.output.draw.image(LOGO, ['%', x=0.5, y=0.33])
            ;; narrative text (lower half)
            loop i, line in TEXT {
                pico.output.draw.text(line, r) where {
                    y = 0.62 + ((i-1) * 0.045)
                    r = ['%', x=0.5, y=y, h=0.025]
                }
            }
            ;; >>> next (bottom-right, inside the frame)
            pico.output.draw.image(NEXT, ['%', x=0.85, y=0.85])
        }
    }
}
