# Plan: Main / Screen Orchestration

`main.atm` sets up the window and hosts the menu (`await Menu()`).
The menu (`menu.atm`) is the persistent base of the screen stack:
it owns the dispatch loop and opens a placeholder `Blank` screen
for each non-exit button.

## Status

- [x] `Blank (text)` placeholder screen (in `main.atm`)
- [x] dispatch loop in `menu.atm`; `main.atm` hosts `Blank`/`Fade`
- [x] snapshot `Fade` transition: backdrop `:snap` on `window`,
  clipped to a box shrinking 1->0 to wipe to `Next` on `world`
  (needed the pico-sdl expert-mode `clip` fix). `await Fade(Blank, text)`.
- [ ] true "new grows from center" (incoming on the front layer,
  per-screen layers) — Future
- [ ] live `Fade` (keep menu untoggled) — Future
- [ ] real screens to replace `Blank` (worldmap, editor, level
  menu, options)

## Current model (menu-owned dispatch)

The menu does not return an id.
It loops: show the 5 buttons, `await :any buttons`,
`match id` -> `await Blank(text)`, re-arm; `:Exit` ends the loop
(and the program).
`Blank (text)` shows centered text until `Escape`.

Accepted simplification: the menu stays alive under `Blank`, so
the parallax background keeps scrolling behind it — no
freeze/transition yet.

## Screen mapping (from C++ `on_click` / Céu `escape`)

| id          | next screen                    | back to menu |
|-------------|--------------------------------|--------------|
| `:Story`    | worldmap (`tutorial.worldmap`) | yes          |
| `:Editor`   | editor (stops music)           | yes          |
| `:Levelsets`| level menu                     | yes          |
| `:Options`  | options (pauses the menu)      | yes          |
| `:Exit`     | quit                           | terminates   |

## Future

- Transition: the original `fade_over` reveals the new screen
  through a centered box growing 0->full (~0.5s).
  A structured-concurrency port (screenshot backdrop + animated
  `scene.clip`) was sketched and shelved — kept simple for now.
- Discarded: returning the id to `main` (CPS); a keep-alive
  `Stack(cur,nxt)` task that froze the menu via `toggle`.
- `:Options` may *pause* the menu (Céu `emit go_options`)
  instead of replacing it.
- Background music `pingus-1.it` start/stop across transitions.
